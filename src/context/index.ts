import { getStores, login, logout, handleModal } from './actions';
import { store, StateProvider} from './store';
 
export { 
  store,
  StateProvider,
  getStores,
  login,
  logout,
  handleModal
};