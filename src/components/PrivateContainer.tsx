import React, { useContext } from 'react';

// const PrivateContainer = ({ component: Component , ...rest }) => {
//   const { state } = useContext(store);
//   return (
//     state.user ? (
//         <Component />
//       ) : (
//           null
//       )
//   );
// };



const PrivateContainer = () => {
  return (
    <div>
      
    </div>
  );
};

export default PrivateContainer;